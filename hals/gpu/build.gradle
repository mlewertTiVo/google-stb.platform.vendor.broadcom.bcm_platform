android {
    signingConfigs {
        release {
            storeFile file(gfxdriver-bcmstb.jks)
            storePassword "broadcom"
            keyAlias "gfxdriver-key"
            keyPassword "broadcom"
        }
    }
    defaultConfig {
        versionCode 1000000
        versionName "initial"
        signingConfig signingConfigs.release
        minSdkVersion 25
        targetSdkVersion 25
    }
}
